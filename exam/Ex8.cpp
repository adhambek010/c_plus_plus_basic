#include <iostream>
using namespace std;
int generate_array(int*,int);
int main()
{
    int length,a,b;
    cout << "Please enter length of array: ";
    cin >> length;
    if(length > 0)
    {
        int *newarray = new int[length];

    }
}
int generate_array(int *arry,int length){
    for (int i = 0; i < length; i++){
        arry[i] =
    }
}
